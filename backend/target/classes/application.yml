spring:
  application:
    name: petshop-backend
  
  datasource:
    url: jdbc:mysql://localhost:3306/petshop_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: admin1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  data:
    redis:
      host: localhost
      port: 6379
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

server:
  port: 8080

# JWT Configuration
jwt:
  secret: cGV0c2hvcC1zZWNyZXQta2V5LWZvci1qd3QtdG9rZW4tMjU2LWJpdHMtc2VjdXJl
  expiration: 86400000 # 24 hours in milliseconds

# Swagger/OpenAPI
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method

# Logging
logging:
  level:
    com.petshop: DEBUG
    org.springframework.security: DEBUG

# Pet Shop Specific Configuration
petshop:
  points:
    earn-rate: 1000   # 1000 VND = 1 điểm
    redeem-value: 100 # 1 điểm = 100 VND
  
  appointment:
    slot-duration: 30  # minutes
    advance-booking-days: 30  # Đặt trước tối đa 30 ngày
    min-booking-hours: 2  # Đặt trước ít nhất 2 giờ
